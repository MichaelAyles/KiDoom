# Manual Plugin Load Test

Please run these commands **one at a time** in KiCad Scripting Console (Tools → Scripting Console) and share the output:

## Test 1: Check if kidoom_plugin.py loads

```python
exec(open('/Users/tribune/Documents/KiCad/9.0/scripting/plugins/kidoom_plugin.py').read())
```

**What to look for:**
- Should print loading messages
- Any error messages?

---

## Test 2: Check if we can import the modules

```python
import sys
plugin_dir = '/Users/tribune/Documents/KiCad/9.0/scripting/plugins/kicad_doom_plugin'
if plugin_dir not in sys.path:
    sys.path.insert(0, plugin_dir)

print("Trying to import config...")
from config import get_doom_binary_path
print("✓ Config imported")

print("Trying to import doom_plugin_action...")
from doom_plugin_action import DoomKiCadPlugin
print("✓ DoomKiCadPlugin imported")
```

**What to look for:**
- Which import fails?
- What's the error message?

---

## Test 3: Check what plugins are registered

```python
from pcbnew import GetActionPlugins

plugins = GetActionPlugins()
print(f"\nRegistered plugins ({len(plugins)}):")
for p in plugins:
    print(f"  - {p.GetName()}")
```

**What to look for:**
- Is "Test Plugin" in the list?
- Is "KiDoom - DOOM on PCB" in the list?

---

## Quick Fix: Try Simple Registration

If the imports work but registration fails, try this simpler version:

```python
import sys
sys.path.insert(0, '/Users/tribune/Documents/KiCad/9.0/scripting/plugins/kicad_doom_plugin')

from doom_plugin_action import DoomKiCadPlugin
plugin = DoomKiCadPlugin()
plugin.register()

print("Plugin registered! Check Tools → External Plugins")
```

---

Please share the output from these tests and I'll identify the exact issue.
